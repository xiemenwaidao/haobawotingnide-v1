---
import type { Work } from "@assets/worksData";
import LinkButton from "./LinkButton.astro";
import { Picture } from "@astrojs/image/components";

interface Props {
    work: Work;
}
const { work } = Astro.props;
---

<li>
    <div class="max-w-full sm:max-w-sm rounded-lg shadow h-full flex flex-col">
        <a href={work.href}>
            <Picture
                src={import(`../assets/img/works/${work.title}.png`)}
                alt={work.title}
                sizes="(max-width: 800px) 100vw, 800px"
                widths={[200, 400, 800]}
                aspectRatio="16:9"
                class="rounded-t-lg"
            />
        </a>
        <div class="flex-grow flex flex-col p-5">
            <a href={work.href}>
                <h5
                    class="mb-2 text-2xl font-bold tracking-tight text-skin-accent hover:underline decoration-dashed"
                >
                    {work.title}
                </h5>
            </a>
            <p class="mb-5 font-normal grow">
                {work.description}
            </p>
            <LinkButton href={work.href} target="_blank" className={"w-fit"}>
                Read more
                <svg xmlns="http://www.w3.org/2000/svg"
                    ><path
                        d="m11.293 17.293 1.414 1.414L19.414 12l-6.707-6.707-1.414 1.414L15.586 11H6v2h9.586z"
                    ></path>
                </svg>
            </LinkButton>
        </div>
    </div>
</li>
